<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/mobilemorph_agent"
        android:label="MobileMorph Agent"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:networkSecurityConfig="@xml/network_security" 
        android:name="com.mobilemorph.agent.AgentApplication"
        xmlns:tools="http://schemas.android.com/tools">

        <!-- Main agent logic -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="MobileMorph Agent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Stealth settings UI -->
        <activity android:name=".StealthSettingsActivity" android:exported="true" />

        <!-- Background service and receivers -->
        <service
            android:name=".services.CommandService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync" />

        <!-- ServerSocket service for ADB port forwarding (Drozer replacement) -->
        <service
            android:name=".services.ServerSocketService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync" />

        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.work.WorkManagerInitializer"
                tools:replace="android:value" />
        </provider>
    </application>
</manifest>
